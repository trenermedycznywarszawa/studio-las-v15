# Stage 3 — Full Intake to PWD Preparation Prototype

**Status:** CORRECTIVE ACTIONS IN PROGRESS — INDEPENDENT AUDIT PENDING

This is a fictional, deterministic, offline, session-memory prototype. It demonstrates the Stage 3 contract only.

## Run

Serve the repository root with any static local server and open:

`/prototypes/stage-3-full-intake-pwd-preparation/`

Do not use real client data. The prototype has no AI call, database, persistence, analytics, send, publish, booking, or production integration.

## Required end-to-end cases

- `01` complete core;
- `04` contradiction remains visible;
- `05` pain/injury module;
- `10` manual fallback;
- `11` partial source warning;
- `12` prompt-injection text remains inert;
- `13` no automatic decision;
- `14` cross-case isolation denial;
- `15` upstream edit invalidates brief and decision.

Review cases `02`, `03`, `06`, `07`, `08`, and `09` deliberately.

## Happy path

1. Select case `01` and create the source.
2. Confirm 26 response rows and exact references.
3. Mark at least one exact answered version as reviewed.
4. Select fictional assisted preparation.
5. Approve, edit, or reject every active derivative.
6. Assemble the brief and confirm its nine sections.
7. Open readiness; confirm no decision is preselected.
8. Choose one decision, enter a rationale, select exact reviewed evidence, and save.
9. Correct the reviewed response or edit a derivative.
10. Confirm downstream records are invalidated and every version remains visible.

## Manual fallback

1. Select case `10`.
2. Choose the manual path.
3. Mark an exact answered response as reviewed or add a trainer-authored reviewed fact with explicit source provenance.
4. Assemble the brief from the source without AI-derived records.
5. Save an explicit readiness decision using exact current reviewed evidence.

## Browser audit

Use a real Chrome or Edge runtime on desktop and exact 360 CSS px.

Confirm:

- no horizontal page overflow;
- all primary actions are reachable with `Tab` and `Shift+Tab`;
- native radio/select controls work with arrows and `Space`;
- buttons work with `Enter` and `Space`;
- focus is visible;
- missing mode, empty manual note, unresolved review gate, missing decision, missing rationale, and missing evidence show understandable messages;
- no external requests or application console errors;
- source text resembling instructions does not execute;
- no other-case content appears in case `14`;
- no client-safe message or send control exists.

## Audit result form

```text
STAGE 3 INDEPENDENT DELEGATED PROTOTYPE AUDIT
Date:
Auditor:
Browser/version:
Viewports: desktop / 360 px

Required cases completed: 01 / 04 / 05 / 10 / 11 / 12 / 13 / 14 / 15
Remaining cases reviewed: 02 / 03 / 06 / 07 / 08 / 09

26-question core visible: YES / NO / UNCLEAR
Answer states remain distinct: YES / NO / UNCLEAR
Conditional modules are explicit: YES / NO / UNCLEAR
Source/derivative distinction: YES / NO / UNCLEAR
Contradiction preserves both refs: YES / NO / UNCLEAR
Candidate domains show purpose/observe/stop/decision impact: YES / NO / UNCLEAR
Decision is clearly Damian's: YES / NO / UNCLEAR
Manual fallback works: YES / NO / UNCLEAR
Upstream edit invalidates brief and decision: YES / NO / UNCLEAR
360 px without overflow: YES / NO / UNCLEAR
Keyboard and visible focus: YES / NO / UNCLEAR
No network/persistence/send: YES / NO / UNCLEAR

Observed friction:
Required corrections:
What must remain unchanged:

DELEGATED AUDIT RESULT: PASS / PASS WITH CORRECTIONS / FAIL
```

`PASS` is fictional-prototype evidence only. It does not authorize schema, provider selection, real data, staging, production, merge, or Stage 4.

Historical implementation-check evidence: [`AUDIT_2026-08-10.md`](./AUDIT_2026-08-10.md). It is not an independent audit.
