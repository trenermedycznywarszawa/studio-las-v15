# Staging Edge Function Evidence

Date: 2026-07-13
Environment: free fictional-data staging only
Project ref: `ulauyoqjoetjqktegeuq`

## Confirmed deployment state

The `client-access` Edge Function was deployed from the PR #9 branch source:

`supabase/functions/client-access/index.ts`

Confirmed non-sensitive deployment metadata:

- slug: `client-access`
- function ID: `f49e3eb3-b01f-4f2b-b7ad-e0a36fc304bb`
- version: `1`
- status: `ACTIVE`
- JWT verification: `true`
- deployed at: `2026-07-13T14:59:14.629Z`
- deployed bundle SHA-256: `2bf959a082bf5df3b1f628e7cbb745be1775601a2423176a811ed8fa848ccd55`
- Edge Function logs after deployment: no error entries returned

No service-role key, JWT, password, invitation link, recovery link, or real client data is recorded here.

## Fail-closed configuration state

Custom staging secrets were not set through this environment:

- `STUDIO_LAS_ALLOWED_ORIGINS`
- `STUDIO_LAS_CLIENT_REDIRECT_URL`

The function code rejects browser requests when no allowed origin is configured and refuses to create a new invitation when no client redirect is configured.

This means deployment is confirmed, but browser invitation readiness is not confirmed.

## Not yet proven

The following still require a local operator/Codex environment with protected Supabase credentials and real HTTP access:

- authenticated invocation with a real trainer JWT,
- owner trainer status action,
- unrelated trainer/client/anonymous rejection through the deployed endpoint,
- exact origin allowlist behavior,
- invitation email delivery,
- actual invitation and recovery callback format,
- password setup and recovery flows,
- link/revoke behavior through the deployed function,
- audit attribution generated by the deployed function rather than direct database RPC tests,
- staging custom SMTP and Auth configuration.

The connected execution environment could not complete an Auth HTTP login request, and its safety layer blocked direct password manipulation. Those safeguards were not bypassed.

## Release interpretation

This document proves only that the reviewed function bundle is deployed and active on free staging with platform JWT verification enabled.

It does not prove that:

- the function is production-ready,
- staging Auth is configured correctly,
- invitation/recovery email flows work,
- the production-sensitive target is migrated,
- mandatory trainer MFA is implemented,
- privacy/RODO requirements are satisfied.

PR #9 must remain draft until the remaining issue #10, #11, and #12 gates pass.
